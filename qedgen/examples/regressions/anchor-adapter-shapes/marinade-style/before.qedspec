// Generated by `qedgen adapt`. Fill in the TODOs to make this verifiable.
// Source: src/lib.rs (program mod: `stake`)

spec Stake

// TODO: replace with the actual lifecycle of your program.
type State
  | Init
  | Active

// Error variants discovered in src/lib.rs (`#[error_code] pub enum StakeError`).
type Error
  | InsufficientStake
  | BelowMinimum

/// `deposit` — method on `Deposit`
/// discovered at: src/instructions/deposit.rs
/// accounts struct: `Deposit` (see `#[derive(Accounts)]`)
handler deposit (lamports : U64) : State.Init -> State.Init {
  // TODO: auth <signer>
  // TODO: accounts { ... }
  // TODO: requires
  // TODO: effect { ... }
}

/// `liquid_unstake` — method on `LiquidUnstake`
/// discovered at: src/instructions/liquid_unstake.rs
/// accounts struct: `LiquidUnstake` (see `#[derive(Accounts)]`)
handler liquid_unstake (shares : U64) : State.Init -> State.Init {
  // TODO: auth <signer>
  // TODO: accounts { ... }
  // TODO: requires
  // TODO: effect { ... }
}

