// Generated by `qedgen adapt`. Fill in the TODOs to make this verifiable.
// Source: src/lib.rs (program mod: `counter`)

spec Counter

// TODO: replace with the actual lifecycle of your program.
type State
  | Init
  | Active

// Error variants discovered in src/instructions/increment.rs (`#[error_code] pub enum CounterError`).
type Error
  | Overflow

/// `initialize` — free-fn forwarder
/// discovered at: src/instructions/initialize.rs
/// accounts struct: `Initialize` (see `#[derive(Accounts)]`)
handler initialize (start : U64) : State.Init -> State.Init {
  // TODO: auth <signer>
  // TODO: accounts { ... }
  // TODO: requires
  // TODO: effect { ... }
}

/// `increment` — free-fn forwarder
/// discovered at: src/instructions/increment.rs
/// accounts struct: `Increment` (see `#[derive(Accounts)]`)
handler increment (delta : U64) : State.Init -> State.Init {
  // TODO: auth <signer>
  // TODO: accounts { ... }
  // TODO: requires
  // TODO: effect { ... }
}

