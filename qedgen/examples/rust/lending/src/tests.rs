// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:55ad3387c76e8476
// Unit tests generated from qedspec.
// These test effects, guards, and properties on a plain state struct.
// No SVM or Quasar runtime required — just `cargo test`.

type Address = [u8; 32];

#[derive(Debug, Clone, PartialEq)]
struct PoolState {
    authority: [u8; 32],
    total_deposits: u64,
    total_borrows: u64,
    interest_rate: u64,
}

impl Default for PoolState {
    fn default() -> Self {
        PoolState {
            authority: [0u8; 32],
            total_deposits: 0u64,
            total_borrows: 0u64,
            interest_rate: 0u64,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum PoolStatus {
    Uninitialized,
    Active,
    Paused,
}

#[derive(Debug, Clone, PartialEq)]
struct LoanState {
    borrower: [u8; 32],
    pool: [u8; 32],
    amount: u64,
    collateral: u64,
}

impl Default for LoanState {
    fn default() -> Self {
        LoanState {
            borrower: [0u8; 32],
            pool: [0u8; 32],
            amount: 0u64,
            collateral: 0u64,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum LoanStatus {
    Empty,
    Active,
    Liquidated,
}

/// Apply `init_pool` effects to state.
fn apply_init_pool(state: &mut PoolState, rate: u64) {
    state.interest_rate = rate;
    state.total_deposits = 0;
    state.total_borrows = 0;
}

/// Apply `deposit` effects to state.
fn apply_deposit(state: &mut PoolState, amount: u64) {
    state.total_deposits += amount;
}

/// Apply `borrow` effects to state.
fn apply_borrow(state: &mut LoanState, amount: u64, collateral: u64) {
    state.amount = amount;
    state.collateral = collateral;
}

/// Apply `repay` effects to state.
fn apply_repay(state: &mut LoanState) {
    state.amount = 0;
    state.collateral = 0;
}

/// Apply `liquidate` effects to state.
fn apply_liquidate(state: &mut LoanState) {
    state.amount = 0;
}

/// Guard predicate for `init_pool`.
fn guard_init_pool(_state: &PoolState, rate: u64) -> bool {
    true
}

/// Guard predicate for `deposit`.
fn guard_deposit(_state: &PoolState, amount: u64) -> bool {
    true
}

/// Guard predicate for `borrow`.
fn guard_borrow(_state: &LoanState, amount: u64, collateral: u64) -> bool {
    true
}

/// Guard predicate for `liquidate`.
fn guard_liquidate(_state: &LoanState) -> bool {
    true
}

#[cfg(test)]
mod tests {
    use super::*;

    // ====================================================================
    // Effect tests — verify state mutations match spec
    // ====================================================================

    #[test]
    fn test_init_pool_effects() {
        let mut state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let rate: u64 = 1;
        apply_init_pool(&mut state, rate);
        assert_eq!(state.interest_rate, rate);
        assert_eq!(state.total_deposits, 0);
        assert_eq!(state.total_borrows, 0);
    }

    #[test]
    fn test_deposit_effects() {
        let mut state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let amount: u64 = 100;
        let pre_total_deposits = state.total_deposits;
        apply_deposit(&mut state, amount);
        assert_eq!(state.total_deposits, pre_total_deposits + amount);
    }

    #[test]
    fn test_borrow_effects() {
        let mut state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        let amount: u64 = 100;
        let collateral: u64 = 1;
        apply_borrow(&mut state, amount, collateral);
        assert_eq!(state.amount, amount);
        assert_eq!(state.collateral, collateral);
    }

    #[test]
    fn test_repay_effects() {
        let mut state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        apply_repay(&mut state);
        assert_eq!(state.amount, 0);
        assert_eq!(state.collateral, 0);
    }

    #[test]
    fn test_liquidate_effects() {
        let mut state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        apply_liquidate(&mut state);
        assert_eq!(state.amount, 0);
    }

    // ====================================================================
    // Guard tests — verify boundary conditions
    // ====================================================================

    #[test]
    fn test_init_pool_guard_accepts_valid() {
        let state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let rate: u64 = 1;
        assert!(guard_init_pool(&state, rate));
    }

    #[test]
    fn test_init_pool_guard_rejects_invalid() {
        let state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let rate: u64 = 0;
        assert!(!guard_init_pool(&state, rate));
    }

    #[test]
    fn test_deposit_guard_accepts_valid() {
        let state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let amount: u64 = 100;
        assert!(guard_deposit(&state, amount));
    }

    #[test]
    fn test_deposit_guard_rejects_invalid() {
        let state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let amount: u64 = 0;
        assert!(!guard_deposit(&state, amount));
    }

    #[test]
    fn test_borrow_guard_accepts_valid() {
        let state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        let amount: u64 = 100;
        let collateral: u64 = 1;
        assert!(guard_borrow(&state, amount, collateral));
    }

    #[test]
    fn test_borrow_guard_rejects_invalid() {
        let state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        let amount: u64 = 0;
        let collateral: u64 = 0;
        assert!(!guard_borrow(&state, amount, collateral));
    }

    #[test]
    fn test_liquidate_guard_accepts_valid() {
        let state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        assert!(guard_liquidate(&state));
    }

    #[test]
    fn test_liquidate_guard_rejects_invalid() {
        let state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        assert!(!guard_liquidate(&state));
    }

    // ====================================================================
    // Property tests — verify invariants hold after effects
    // ====================================================================

    #[test]
    fn test_init_pool_preserves_pool_solvency() {
        let mut state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let rate: u64 = 1;
        apply_init_pool(&mut state, rate);
        // Property: pool solvency must hold after init_pool
        assert!(state.total_deposits >= state.total_borrows, "pool_solvency must hold after init_pool");
    }

    #[test]
    fn test_deposit_preserves_pool_solvency() {
        let mut state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let amount: u64 = 100;
        apply_deposit(&mut state, amount);
        // Property: pool solvency must hold after deposit
        assert!(state.total_deposits >= state.total_borrows, "pool_solvency must hold after deposit");
    }

    // pool_solvency.borrow skipped — borrow operates on LoanState, not PoolState

    // pool_solvency.repay skipped — repay operates on LoanState, not PoolState

    // pool_solvency.liquidate skipped — liquidate operates on LoanState, not PoolState

    // ====================================================================
    // Unchanged field tests — fields not in effects must not change
    // ====================================================================

    #[test]
    fn test_deposit_unchanged_fields() {
        let mut state = PoolState {
            authority: [1u8; 32],
            total_deposits: 0,
            total_borrows: 0,
            interest_rate: 0,
        };
        let amount: u64 = 100;
        let pre_total_borrows = state.total_borrows.clone();
        let pre_interest_rate = state.interest_rate.clone();
        apply_deposit(&mut state, amount);
        assert_eq!(state.total_borrows, pre_total_borrows, "total_borrows must not change after deposit");
        assert_eq!(state.interest_rate, pre_interest_rate, "interest_rate must not change after deposit");
    }

    #[test]
    fn test_liquidate_unchanged_fields() {
        let mut state = LoanState {
            borrower: [1u8; 32],
            pool: [1u8; 32],
            amount: 100,
            collateral: 0,
        };
        let pre_collateral = state.collateral.clone();
        apply_liquidate(&mut state);
        assert_eq!(state.collateral, pre_collateral, "collateral must not change after liquidate");
    }

    // ====================================================================
    // State machine tests — verify lifecycle transitions
    // ====================================================================

    #[test]
    fn test_init_pool_transition_uninitialized_to_active() {
        // init_pool requires status == Uninitialized and moves to Active
        assert_ne!(PoolStatus::Uninitialized, PoolStatus::Active, "init_pool changes status");
        let _pre = PoolStatus::Uninitialized;
        let _post = PoolStatus::Active;
        // AGENT: verify handler transitions status from _pre to _post
    }

    #[test]
    fn test_deposit_transition_active_to_active() {
        // deposit requires status == Active and moves to Active
        assert_eq!(PoolStatus::Active, PoolStatus::Active, "deposit is a self-transition");
        let _pre = PoolStatus::Active;
        let _post = PoolStatus::Active;
        // AGENT: verify handler transitions status from _pre to _post
    }

    #[test]
    fn test_borrow_transition_empty_to_active() {
        // borrow requires status == Empty and moves to Active
        assert_ne!(LoanStatus::Empty, LoanStatus::Active, "borrow changes status");
        let _pre = LoanStatus::Empty;
        let _post = LoanStatus::Active;
        // AGENT: verify handler transitions status from _pre to _post
    }

    #[test]
    fn test_repay_transition_active_to_empty() {
        // repay requires status == Active and moves to Empty
        assert_ne!(LoanStatus::Active, LoanStatus::Empty, "repay changes status");
        let _pre = LoanStatus::Active;
        let _post = LoanStatus::Empty;
        // AGENT: verify handler transitions status from _pre to _post
    }

    #[test]
    fn test_liquidate_transition_active_to_liquidated() {
        // liquidate requires status == Active and moves to Liquidated
        assert_ne!(LoanStatus::Active, LoanStatus::Liquidated, "liquidate changes status");
        let _pre = LoanStatus::Active;
        let _post = LoanStatus::Liquidated;
        // AGENT: verify handler transitions status from _pre to _post
    }

}
