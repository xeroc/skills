# QEDGen manifest for the percolator example.
#
# This spec has no external interface imports today. Keep the manifest present
# so example discovery and codegen-drift checks can treat all examples
# uniformly.

[dependencies]
