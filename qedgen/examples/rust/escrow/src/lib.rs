// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:382fd5d9b1996d0e
#![no_std]

use quasar_lang::prelude::*;

mod instructions;
use instructions::*;
pub mod events;
pub mod errors;
pub mod state;

declare_id!("11111111111111111111111111111111");

#[program]
mod escrow {
    use super::*;

    /// Initializer deposits tokens and opens the escrow
    #[instruction(discriminator = 0)]
    pub fn initialize(ctx: Ctx<Initialize>, deposit_amount: u64, receive_amount: u64) -> Result<(), ProgramError> {
        ctx.accounts.handler(deposit_amount, receive_amount, &ctx.bumps)
    }

    /// Taker completes the trade by sending their tokens
    #[instruction(discriminator = 1)]
    pub fn exchange(ctx: Ctx<Exchange>) -> Result<(), ProgramError> {
        ctx.accounts.handler(&ctx.bumps)
    }

    /// Initializer reclaims their deposit and closes the escrow
    #[instruction(discriminator = 2)]
    pub fn cancel(ctx: Ctx<Cancel>) -> Result<(), ProgramError> {
        ctx.accounts.handler(&ctx.bumps)
    }

}
// ---- END GENERATED ----
