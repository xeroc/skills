// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:f14e30192758cfbd
// Unit tests generated from qedspec.
// These test effects, guards, and properties on a plain state struct.
// No SVM or Quasar runtime required — just `cargo test`.

type Address = [u8; 32];

#[derive(Debug, Clone, PartialEq)]
struct EscrowState {
    initializer: [u8; 32],
    initializer_token_account: [u8; 32],
    taker: [u8; 32],
    initializer_amount: u64,
    taker_amount: u64,
    escrow_token_account: [u8; 32],
}

impl Default for EscrowState {
    fn default() -> Self {
        EscrowState {
            initializer: [0u8; 32],
            initializer_token_account: [0u8; 32],
            taker: [0u8; 32],
            initializer_amount: 0u64,
            taker_amount: 0u64,
            escrow_token_account: [0u8; 32],
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum Status {
    Uninitialized,
    Open,
    Closed,
}

/// Apply `initialize` effects to state.
fn apply_initialize(state: &mut EscrowState, deposit_amount: u64, receive_amount: u64) {
    state.initializer_amount = deposit_amount;
    state.taker_amount = receive_amount;
    state.initializer_token_account = initializer_ta.pubkey;
}

/// Guard predicate for `initialize`.
fn guard_initialize(_state: &EscrowState, deposit_amount: u64, receive_amount: u64) -> bool {
    true
}

/// Guard predicate for `exchange`.
fn guard_exchange(_state: &EscrowState) -> bool {
    true
}

/// Guard predicate for `cancel`.
fn guard_cancel(_state: &EscrowState) -> bool {
    true
}

#[cfg(test)]
mod tests {
    use super::*;

    // ====================================================================
    // Effect tests — verify state mutations match spec
    // ====================================================================

    #[test]
    fn test_initialize_effects() {
        let mut state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        let deposit_amount: u64 = 100;
        let receive_amount: u64 = 100;
        apply_initialize(&mut state, deposit_amount, receive_amount);
        assert_eq!(state.initializer_amount, deposit_amount);
        assert_eq!(state.taker_amount, receive_amount);
        assert_eq!(state.initializer_token_account, initializer_ta.pubkey);
    }

    // ====================================================================
    // Guard tests — verify boundary conditions
    // ====================================================================

    #[test]
    fn test_initialize_guard_accepts_valid() {
        let state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        let deposit_amount: u64 = 100;
        let receive_amount: u64 = 100;
        assert!(guard_initialize(&state, deposit_amount, receive_amount));
    }

    #[test]
    fn test_initialize_guard_rejects_invalid() {
        let state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        let deposit_amount: u64 = 0;
        let receive_amount: u64 = 0;
        assert!(!guard_initialize(&state, deposit_amount, receive_amount));
    }

    #[test]
    fn test_exchange_guard_accepts_valid() {
        let state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        assert!(guard_exchange(&state));
    }

    #[test]
    fn test_exchange_guard_rejects_invalid() {
        let state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        assert!(!guard_exchange(&state));
    }

    #[test]
    fn test_cancel_guard_accepts_valid() {
        let state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        assert!(guard_cancel(&state));
    }

    #[test]
    fn test_cancel_guard_rejects_invalid() {
        let state = EscrowState {
            initializer: [1u8; 32],
            initializer_token_account: [1u8; 32],
            taker: [1u8; 32],
            initializer_amount: 100,
            taker_amount: 100,
            escrow_token_account: [1u8; 32],
        };
        assert!(!guard_cancel(&state));
    }

    // ====================================================================
    // Unchanged field tests — fields not in effects must not change
    // ====================================================================

    // ====================================================================
    // State machine tests — verify lifecycle transitions
    // ====================================================================

    #[test]
    fn test_initialize_transition_uninitialized_to_open() {
        // initialize requires status == Uninitialized and moves to Open
        assert_ne!(Status::Uninitialized, Status::Open, "initialize changes status");
        let _pre = Status::Uninitialized;
        let _post = Status::Open;
        // AGENT: verify handler transitions status from _pre to _post
    }

    #[test]
    fn test_exchange_transition_open_to_closed() {
        // exchange requires status == Open and moves to Closed
        assert_ne!(Status::Open, Status::Closed, "exchange changes status");
        let _pre = Status::Open;
        let _post = Status::Closed;
        // AGENT: verify handler transitions status from _pre to _post
    }

    #[test]
    fn test_cancel_transition_open_to_closed() {
        // cancel requires status == Open and moves to Closed
        assert_ne!(Status::Open, Status::Closed, "cancel changes status");
        let _pre = Status::Open;
        let _post = Status::Closed;
        // AGENT: verify handler transitions status from _pre to _post
    }

}
