# ---- GENERATED BY QEDGEN ---- spec-hash:{SPEC_HASH}
#
# TODO: configure program runtime dependencies.
#
# The generated `src/*.rs` uses `use prelude::*;` and Anchor-style macros
# (`#[program]`, `#[derive(Accounts)]`, `declare_id!`, `#[account]`, `#[event]`,
# `#[error_code]`). Add the framework crate that provides them — e.g.:
#
#     anchor-lang = "0.32.1"
#     anchor-spl  = "0.32.1"        # when the spec declares token transfers
#
# Then replace the `use prelude::*;` line at the top of each generated file
# with `use anchor_lang::prelude::*;` (or your framework's equivalent).
#
# `qedgen-macros` is the compile-time drift-detection proc-macro used by
# `#[qed(verified, spec_hash = "...")]`. Add it from the same release tag
# that generated this scaffold:
#
#     qedgen-macros = { git = "https://github.com/qedgen/solana-skills", tag = "v{QEDGEN_VERSION}" }
#
# See `references/cargo-configure.md` in the qedgen release for the full
# walkthrough, or `qedgen --help` for the current release tag.

[package]
name = "{PROGRAM_NAME}"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
client = []
debug = []

[dependencies]
# TODO: runtime framework — see header comment.
# anchor-lang = "0.32.1"

# TODO: drift-detection macros — pin to the qedgen release tag that generated this scaffold.
# qedgen-macros = { git = "https://github.com/qedgen/solana-skills", tag = "v{QEDGEN_VERSION}" }
