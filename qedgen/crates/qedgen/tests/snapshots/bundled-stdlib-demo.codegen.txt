--- Cargo.toml ---
# ---- GENERATED BY QEDGEN ---- spec-hash:2df962680fe8d550

[package]
name = "pooldemo"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
client = []
debug = []

[dependencies]
anchor-lang = "0.32.1"
anchor-spl = "0.32.1"
qedgen-macros = { git = "https://github.com/qedgen/solana-skills", tag = "v2.47.0" }

[dev-dependencies]
proptest = "1"

[workspace]

--- src/errors.rs ---
// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:4b85ccaa7b7d15ba
use anchor_lang::prelude::*;

#[error_code]
pub enum PoolDemoError {
    InvalidAmount = 0,
    WrongState = 1,
    MathOverflow = 2,
    InvalidLifecycle = 3,
}
// ---- END GENERATED ----

--- src/guards.rs ---
// ---- GENERATED BY QEDGEN — DO NOT EDIT — regenerated from .qedspec ----
//! Per-handler guard checks derived from the `.qedspec`.
//! Called from user-owned `instructions/<name>::handler` before
//! business logic; keep guard logic here, policy-free logic there.

#![allow(
    unused_variables,
    unused_imports,
    dead_code,
    clippy::too_many_arguments
)]

use crate::errors::*;
use crate::state::*;
use crate::*;
use anchor_lang::prelude::*;

/// Guards for `initialize`.  
/// Generated from the `requires` clauses of the spec handler block.
pub fn initialize(ctx: &Initialize, initial: u64) -> Result<()> {
    // requires: initial > 0
    if !(initial > 0) {
        return Err(PoolDemoError::InvalidAmount.into());
    }
    Ok(())
}

/// Guards for `deposit`.  
/// Generated from the `requires` clauses of the spec handler block.
pub fn deposit(ctx: &Deposit, amount: u64) -> Result<()> {
    // requires: amount > 0
    if !(amount > 0) {
        return Err(PoolDemoError::InvalidAmount.into());
    }
    Ok(())
}

// ---- END GENERATED ----

--- src/instructions/deposit.rs ---
// User-owned. Regenerating the spec does NOT overwrite this file.
// Guard checks live in the sibling `crate::guards` module and ARE
// regenerated on every `qedgen codegen`. Drift between the spec
// handler block and the `spec_hash` below fires a compile_error!
// via the `#[qed(verified, ...)]` macro.

use crate::errors::*;
use crate::guards;
use crate::Deposit;
use anchor_lang::prelude::*;
use qedgen_macros::qed;

impl Deposit {
    #[qed(
        verified,
        spec = "../pool.qedspec",
        handler = "deposit",
        hash = "b99d8daf7723f6e9",
        spec_hash = "56d9069493858422"
    )]
    #[inline(always)]
    pub fn handler(&self, amount: u64) -> Result<()> {
        guards::deposit(self, amount)?;
        // Spec effect (needs fill): pool_balance add amount
        // Spec call: Token.transfer(amount=amount) — agent fill: Anchor does not fully mechanize this CPI shape; fill the complete invocation
        todo!("fill non-mechanical effects, events, transfers, calls")
    }
}

--- src/instructions/initialize.rs ---
// User-owned. Regenerating the spec does NOT overwrite this file.
// Guard checks live in the sibling `crate::guards` module and ARE
// regenerated on every `qedgen codegen`. Drift between the spec
// handler block and the `spec_hash` below fires a compile_error!
// via the `#[qed(verified, ...)]` macro.

use crate::guards;
use crate::Initialize;
use anchor_lang::prelude::*;
use qedgen_macros::qed;

impl Initialize {
    #[qed(
        verified,
        spec = "../pool.qedspec",
        handler = "initialize",
        hash = "573e6946bf2ddb65",
        spec_hash = "3b68f0bcd07ddf5a"
    )]
    #[inline(always)]
    pub fn handler(&self, initial: u64) -> Result<()> {
        guards::initialize(self, initial)?;
        // Spec effect (needs fill): pool_balance set initial
        todo!("fill non-mechanical effects, events, transfers, calls")
    }
}

--- src/instructions/mod.rs ---
// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:6611bab7cd6f24d3
pub mod deposit;
pub mod initialize;
// ---- END GENERATED ----

--- src/lib.rs ---
// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:798efe9c54ed36fb
#![allow(unexpected_cfgs)]

use anchor_lang::prelude::*;

pub mod errors;
pub mod guards;
mod instructions;
pub mod state;

declare_id!("11111111111111111111111111111111");

#[program]
pub mod pooldemo {
    use super::*;

    pub fn initialize(ctx: Context<Initialize>, initial: u64) -> Result<()> {
        ctx.accounts.handler(initial)
    }

    pub fn deposit(ctx: Context<Deposit>, amount: u64) -> Result<()> {
        ctx.accounts.handler(amount)
    }
}

// `#[derive(Accounts)]` structs live at the crate root so the
// Anchor `#[program]` macro can resolve them via `crate::*`.
// The handler impl blocks live next to the (always-regenerated)
// guard module in `instructions/<name>.rs`.
use crate::state::*;

#[derive(Accounts)]
pub struct Initialize {}

#[derive(Accounts)]
pub struct Deposit {}
// ---- END GENERATED ----

--- src/state.rs ---
// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:c2fd96777ac0a20b
use anchor_lang::prelude::*;

#[account]
#[derive(InitSpace)]
pub struct PoolDemoAccount {
    pub pool_balance: u64,
    pub status: u8,
}

/// Program lifecycle states.
#[derive(Clone, Copy, PartialEq, Eq)]
#[repr(u8)]
pub enum Status {
    Uninitialized = 0,
    Open = 1,
}
// ---- END GENERATED ----

